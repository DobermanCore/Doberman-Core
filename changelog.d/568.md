- **Cursor native-hooks adapter (experimental, slice 1).** `doberman hook cursor` gates Cursor's
  `preToolUse` / `beforeShellExecution` / `beforeMCPExecution` / `beforeReadFile` events on the shared
  spine: explicit `allow` on PASS, `deny` **and** exit code 2 on BLOCK, Doberman's own action-bound
  challenge on AUTH (never Cursor's `ask`), fail-closed on malformed or BOM-prefixed input, the Claude
  Code output scan over `beforeReadFile` content, and channel-aware single-flight. `.cursor` /
  `.cursor/hooks.json` join the hard-blocked control plane and `.cursor/**` the AUTH set; Cursor is a
  parity column. Manual `hooks.json` registration for now (`adapters/cursor/README.md`); the
  installer, heartbeat and doctor checks follow. Also fixes the single-flight marker filename on
  Windows, where the `hmac:` prefix made the peer channel re-evaluate (a doubled AUTH prompt). Refs #202.
