- `ActivityEvent.target_path_class` now accepts wildcarded Windows drive-letter path classes (`C:/Users/x/.aws/*` and the backslash spelling) while still rejecting the concrete filename (`C:/Users/x/.aws/credentials`). Before this, every absolute-target file action on Windows produced a class the activity bus refused (#704)
