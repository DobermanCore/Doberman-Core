- **Every entry-point plugin seam is opt-in by name.** `doberman plugins enable <name>` (per-user
  allowlist, `doberman plugins list`/`disable`) gates rules, detectors, auth providers, and every other
  registry group — installing a package is never enough on its own. An auth provider's approval is also
  always additive: the built-in local provider is asked too, for every tier, not just role elevation
  (#520)
