- **`doberman setup` now has an honest end.** When the closing doctor pass finds a critical (most
  commonly `doberman` not yet on PATH), the wizard prints `-- Setup incomplete --`, skips the
  "Hooks written. Doberman activates..." claim, and exits `1` — never `complete` on top of a
  diagnostic that says tool calls go unmediated. `doberman status` marks an installed hook
  `(not on PATH)` when the same check fails.
- **Every wired host now ends on the same verification ritual.** Claude Code gets the "Verify it's
  live: ask your agent to read .env and confirm it is blocked." line Codex and the MCP proxy
  already had; the run's last two lines are that check and `doberman demo --fast`, with
  `doberman uninstall-hooks` moved just above them. A re-run whose hook files are already correct
  prints `already wired: <path>` instead of `wrote <path>`.
- **`doberman setup --dry-run`** previews the mode, the preference weights, and every file it would
  write, with nothing persisted (mirrors `install-hooks --dry-run`). `--global` now asks
  `[y/N]` before writing your real home directory (or, under `--yes`, prints the exact path first).
- **Output wraps to your terminal width** (60-120 columns) instead of printing 300+-char lines, and
  the security-mode summary, doctor severity, and section titles are colored (green/cyan/yellow/
  bright-red, NO_COLOR-respecting) instead of one shade of green.
- **Setup always reports telemetry state** in its closing summary (`Telemetry: on/off`), even when
  the one-time notice already fired on `--help`. "trifecta actions" is now glossed inline in the
  mode copy, and the wizard ends with a `Docs: docs/SETUP.md` pointer.
- **A run that only wired `mcp`/`openclaw` now prints `-- Setup pending --`, never `complete`.**
  Nothing is running yet until you paste the printed block into your client and restart it, so the
  header, the "Hooks written" claim, and the closing verify line all say so honestly (exit stays
  `0` - this isn't an error). `doberman demo --fast` is still offered on this path, since the demo
  runs in-process either way.
- **The optional closing demo prompt can no longer fail a succeeded setup.** A closed/exhausted
  stdin (or Ctrl-C) there now falls back to the same static `Next: \`doberman demo --fast\``
  pointer `--yes` prints, instead of raising (a leading blank line keeps it off the confirm
  prompt's own newline-less line).
- **Telemetry gets its own `-- Telemetry --` section**, moved to run after hosts are wired and
  before the doctor pass, with a one-line explanation of what is sent - no longer wedged between
  the mode and preference-tuning prompts.
- **The incomplete path ends on the remedy, not the success epilogue.** A critical doctor finding
  now prints only `Docs:`/`Check health:` and a red close naming the failing check and the first
  sentence of its detail - Telemetry/Next step/Change-your-mind no longer print underneath a run
  that isn't actually protecting anything yet.
- **Long lines wrap consistently with the section rule** (both capped at 78 columns), a `\S+` token
  containing `\` or `/` (a Windows path, a URL) is never split mid-token, and a doctor
  remediation's continuation lines now indent under the text instead of the `- ` marker.
- **`--help` leads with "Getting started"** - every command now carries an explicit help panel, so
  the unlabeled grab-bag Typer/Rich printed first (2fa, taint, plugins, ...) is gone.
- **`doberman status` leads with a `Protected: yes` / `Protected: no - <reason>` headline** (hooks
  installed for at least one host and `doberman` resolvable on PATH).
- **`--yes --mode light` over a higher mode now says so on stdout, not just stderr** -
  `Mode: balanced (requested light; not lowered - a possession factor is required, run
  'doberman mode light')` survives `2>/dev/null` and names the exact command to run.
- **`setup --dry-run` writes nothing, including telemetry state** - it previews
  `[dry-run] would record telemetry consent: on/off` instead of touching `~/.doberman/telemetry.json`
  the way every other `--dry-run` command already writes nothing.
- Re-prompt errors (a mistyped host or mode choice) now print `error: ...` on stderr instead of an
  unmarked line mixed into stdout.
- **One primary next step, not seven equal-weight lines.** The success epilogue now closes with
  `Verify it's live: ...`, then a single bold `Next: \`doberman demo --fast\`` (interactive: the
  inline offer), then one muted `Also: ...` line compactly listing `doberman password set` (if no
  factor is set), `doberman doctor`, `doberman uninstall-hooks`, the docs URL, and `doberman
  telemetry off`. Codex now gets the "Verify it's live" epilogue line too, not only its own
  mid-wiring TRUST ritual.
- **The write is the visible event.** `wrote <path>` / `already wired: <path>` render bold
  (NO_COLOR-guarded), and the `Hosts:` summary says `already wired` when that's what happened,
  for both Claude and Codex.
- **A step counter on every interactive prompt.** Section titles carry `[step N of M]`
  (`-- Hosts [1 of 6] ----`) computed from what this run will actually show; `--yes` never shows one
  since nothing is prompted.
- **Preference tuning is honest about bad input.** An invalid weight re-prompts exactly like every
  other field (`error: 'abc' is not a number between 0 and 1 - try again`, on stderr), never a raw
  `could not convert string to float`. The summary now names exactly the dimensions that changed
  (`custom (tuned: confidentiality, blast_radius)`), or falls back to the preset name when nothing
  changed after all.
- **Every wizard menu (hosts, mode, weight tuning) has an exit and a cap.** `q`/`quit` aborts
  cleanly (`Aborted - nothing written.`, exit 1); five invalid answers in a row aborts the same way
  instead of looping forever; a closed/exhausted stdin at any menu gets this same message instead
  of a bare Click `Aborted!`.
- **`setup`'s pending case gets its own exit code, `3`** (documented in `docs/CLI.md`'s exit-code
  table: `0` complete, `1` incomplete/critical, `2` usage, `3` pending manual step) - distinct from
  both a fully-live `0` and a broken `1`; the header stays `-- Setup pending --`.
- **The doctor critical prints once.** The doctor block still prints the full `- name: detail`;
  the incomplete path's red close now references it by name only (`Not protecting this repo yet:
  fix 'Hook command' above, then run 'doberman doctor'.`) instead of repeating a truncated copy.
- **Consistency pass:** the mode menu now uses `[N]` numbering like the hosts menu (was `N)`); the
  telemetry explanation and its docs pointer wrap to 78 columns and the pointer is now a full GitHub
  URL; `status`'s `Protected: no - ... not on PATH` headline names the fix (`doberman doctor`);
  `--host vscode` now rejects before the welcome banner, not after.
- **`blast_radius` and "reason payloads" are glossed inline** where they appear as raw jargon
  (`blast_radius=0.50 (actions affecting many targets)`; "reason payloads (the structured
  why-blocked details)"), joining "trifecta" and "possession factor", which already had one.
- **`setup --dry-run` with no `--yes` and no TTY on stdin** now behaves like `--yes --dry-run`
  (defaults, preview, exit 0) instead of hitting EOF at the first prompt, and says so
  (`stdin is not a terminal - using defaults for the preview`); a real TTY keeps the prompts.
- **The abort message on 'q'/'quit' is now honest about what's already saved.** Aborting during
  weight tuning - after the mode was already persisted this run - now says
  `Aborted - security mode <m> was already saved (no hooks written); run 'doberman mode <other>' to
  change it, which needs your possession factor to lower.` instead of the generic (and false)
  `Aborted - nothing written.`, which is still exactly what aborting at the hosts/mode menus prints.
- **`status` and `doctor` now share one output grammar.** Both are grouped into `-- Hooks --`,
  `-- Policy --`, `-- Auth --`, `-- Health --` sections; `status` adds a `Policy version:`
  line (shortened to `pv1:f67ef2bc...`, the full hash under `--json` only) and `doctor`'s closing
  line now names every failing check (`2 critical: Hook command, Config`) instead of a bare count.
  A bad `--mode` now rejects before the welcome banner too, same as a bad `--host` already did.
- **Telemetry mentions itself at most twice per run.** `--yes` no longer prints the full
  explanation twice (once from the generic first-run notice, once from the wizard's own section) -
  the `-- Telemetry --` section shows a compact `Telemetry: on/off` line instead, and `setup --help`
  no longer prints the notice above its usage text. New `--no-telemetry` flag turns telemetry off
  for the run and persists it, same as `doberman telemetry off`.
- **The last three long lines now wrap with a hanging indent** (the mode summary line, the
  telemetry summary line, and `--dry-run`'s mode preview line), matching every other wrapped line
  in the wizard.
- **Every menu prompt is now discoverable as quittable**: hosts, mode, and each tuning weight all
  end in `(q to quit)`.
- **No false "Hooks written." on a re-run that wrote nothing.** When every hooks-kind host was
  already wired, the closing line now reads `Hooks already in place. Doberman activates when you
  restart your session.` instead of claiming a write that didn't happen.
- **The step counter now covers the closing demo offer too** (`[N of M]` where `M` includes
  "Demo"), instead of stopping at "Doctor" one step early.
- **The doctor `Hook command` remedy now names a concrete directory** (`Add <dir> to PATH`) instead
  of a generic "put the install's bin dir on PATH", ahead of the existing `pipx install` and
  `uninstall-hooks` remedies.
- **Tuning prompts explain the 0/1 scale** (`0 = never ask, 1 = ask on every risky action` for
  interruption tolerance, and the equivalent "never/always escalate" framing for the other three),
  and the prompt itself now speaks the dimension as words (`interruption tolerance`, not
  `interruption_tolerance`).
- **`setup --help` documents its exit codes** (`0`/`1`/`3`), and the top-level `hook` group's help
  text now mentions Codex CLI alongside Claude Code.
- Fixed a double-blank-line bug that showed up whenever every optional prompt between two sections
  was skipped (flags/`--yes`) - most visibly right after the welcome banner on an mcp/openclaw-only
  `--yes` run, and between the `Hosts:` summary and the telemetry line on a pending run.
- The closing "see it work?" demo prompt no longer carries its own `Next:` label (only the final
  printed line does); `--host openclaw`-only pending runs get OpenClaw-accurate wording instead of
  the mcp "paste the block" text; Codex's own mid-wiring TRUST ritual no longer duplicates the
  "Verify it's live" pointer the epilogue already gives every wired host.
- A repeated menu prompt (after a bad answer) now starts on its own stdout line instead of gluing
  onto the previous prompt's un-terminated text.
- `doctor`'s remaining em-dashes (the 2FA/Password "not set" warnings, among others) are now ASCII,
  and the cp1252-safety test now covers `doctor`'s output too.
- **No more raised-mode trap.** The chosen security mode and preferences are now persisted only
  after per-host wiring succeeds, never before - an abort or crash anywhere earlier in the wizard
  (hosts, mode, preference tuning, the `--global` confirm) genuinely writes nothing, so
  `Aborted - nothing written.` is honest on every one of those paths. `doberman mode` on a refused
  lowering now prints why (`error: lowering needs a possession factor - run 'doberman password set'
  first, then retry`) instead of the old bare `error: mode change denied; unchanged`.
- **Every prompt in the wizard now shares one abort discipline**, including the telemetry consent
  question and the `--global`/per-host scope confirms (previously bare `typer.confirm` calls that
  could raise Click's own `Aborted!`). Aborting at the telemetry prompt - reached after mode/prefs/
  hooks are already persisted - names what was actually written (e.g. `Aborted - security mode
  balanced, preferences, hooks (<path>/.claude/settings.json) already written; nothing else was.`)
  instead of the generic "nothing written".
- Every `adapters/openclaw/README.md` mention in the CLI (dry-run preview, the OpenClaw wiring
  block, the `Hosts:` summary, and the pending epilogue) is now the live GitHub URL, not a bare
  relative path that resolves to nothing outside a cloned working tree.
- **`--host all` no longer shows the same step number four times.** Each host actually being wired
  now gets its own `[N of M]` slot in the interactive step counter, instead of every per-host
  section sharing one "Wiring" slot.
- **`--dry-run` now has a closing line**: `Dry run - nothing written. Re-run without --dry-run to
  apply.`
- **The `Also:` line no longer competes with itself.** It now holds at most three items -
  `doberman doctor`, `doberman uninstall-hooks` (when a hooks-kind host was wired), and the docs URL
  - `doberman password set` moves into `doctor`'s own advice (its Password check already names it)
  and `doberman telemetry off` moves into the Telemetry section's own line, so neither is repeated.
- **`wrote .../already wired: ...` under `--yes` now lives under a `-- Wiring --` section header**,
  matching the interactive path instead of printing with no header at all.
- **The pending path (mcp/openclaw) no longer advertises the in-process demo as proof the install
  works.** Its bold `Next:` is now the verify-after-paste step; `doberman demo --fast` moves to the
  low-key `Also:` line.
- **`status`'s `Version:` line moves below the `Protected: ...` headline**, into the `-- Policy --`
  section right after `Policy version:`; the setup wizard's own `Prefs:` line now restates the four
  weight numbers alongside the source label, matching `status`'s own `Prefs:` line.
- **The root `Policy` help panel splits in two.** `Policy` now holds only `mode`/`review`/`role`;
  everything else (`enforcement`, `prefs`, `egress-velocity`, `message-tone`, `memory`,
  `policy-history`, `policy-versions`, plus `taint`/`tools` moved out of the equally overloaded
  `Advanced` panel) moves to a new `Policy internals` panel. `Getting started` still leads.
- A weight-tuning re-prompt under a piped/non-tty stdin no longer glues the next dimension's
  description onto the end of the previous prompt's un-terminated line.
