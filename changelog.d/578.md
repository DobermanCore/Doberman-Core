- Security: the untrusted-value echo tripwire now fingerprints mail addresses written as `[at]`/`(dot)`, bare `at`/`dot` words, or spaced `@`/`.` in an untrusted read, so a later send to the plain address raises `untrusted_value_echo` the same way a literal one does; the de-obfuscation is bounded (linear on adversarial input) and its coverage ceiling is documented (#578).
