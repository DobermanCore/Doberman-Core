- **A single-use elevation is claimed atomically before the forward.** Two overlapping calls covered
  by one single-use grant now release exactly once; the loser is blocked with the new
  `single_use_elevation_unclaimable` reason code, and a claim that cannot be recorded denies (#522)
